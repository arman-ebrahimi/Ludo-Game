import {GameBoard, StartPage} from "../components";

export const GamePage = () => {
    return(
        <div className="game-page">
            <GameBoard />
            <StartPage />
        </div>
    )
}